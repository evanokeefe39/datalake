"""Instagram core — scrape, bronze, silver and labels for the IG source.

`bnz/` lands raw Apify payloads; `slv/` builds the normalized silver tables
and the label pass; `gld/` is reserved for domain-scoped gold (cross-domain
marts live in `serving/`).
"""
