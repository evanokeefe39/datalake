"""tiktok_enriched/slv — silver enrichment payloads (skeleton).

TikTok prompts, output schemas, per-workload mappings, validation overlays
(caption bounds; NO carousel rule — short-form video + images only),
checks.py. Engine machinery lives in defs/engine/ and is shared — this
package adds payloads, never orchestration.
"""
