"""Pipeline plumbing: resources, paths, the DuckDB catalog, schedules.

The name used to be `common` — a junk drawer. Everything here is something
the pipeline needs to run rather than something it computes.
"""
