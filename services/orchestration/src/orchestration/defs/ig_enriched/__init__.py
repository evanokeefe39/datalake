"""Instagram enrichment payloads — prompts, output schemas, silver mappings.

No engine machinery lives here: everything reusable across domains is in
`engine/`. `bnz/` is empty by design (landing is generic), and `gld/` is
empty because gold is cross-domain.
"""
