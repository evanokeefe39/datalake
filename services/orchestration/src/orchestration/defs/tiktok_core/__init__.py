"""tiktok_core — future domain package (skeleton, pre-reorg).

Will hold the TikTok scrape → bronze → silver → labels assets, mirroring
ig_core/. Same rules as youtube_core: clients in defs/integration/, roster
rows via packages/opsdb (platform='tiktok'), no engine imports.
"""
