"""tiktok_core/slv — silver layer (skeleton).

Flat files named after the table family they build: posts.py
(silver_tt_posts), sounds.py if sound metadata earns its own table,
labels.py, checks.py. No subdirectories inside the layer.
"""
