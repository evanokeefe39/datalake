"""tiktok_core/bnz — bronze layer (skeleton).

TikTok scrape assets (the equivalent of ig_posts_raw) + ScrapeConfig.
Client in defs/integration/, roster rows via packages/opsdb
(platform='tiktok').
"""
