"""youtube_core/slv — silver layer (skeleton).

Flat files named after the table family they build: videos.py
(silver_yt_videos), channels.py, labels.py, checks.py. No subdirectories
inside the layer; split by table if a file outgrows ~400 lines.
"""
