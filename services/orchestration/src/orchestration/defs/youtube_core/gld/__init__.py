"""youtube_core/gld — gold layer (skeleton).

Domain-scoped gold only. Cross-platform marts, dims, and canonical metric
views live in defs/serving/ — if a table joins across platforms it does not
belong here. An empty gld/ is the correct steady state for most domains.
"""
