"""youtube_core/bnz — bronze layer (skeleton).

Scrape assets land here: the YouTube equivalent of ig_posts_raw (video +
channel metadata from the source API/actor) plus its ScrapeConfig. Client
code belongs in defs/integration/, roster rows come from packages/opsdb
(platform='youtube').
"""
